'use client';import {useEffect,useState} from 'react';import {campaignTiming} from '../../../lib/real-billboard';
export default function LiveCampaign({campaign}:{campaign:any}){const [now,setNow]=useState(new Date());useEffect(()=>{const id=setInterval(()=>setNow(new Date()),1000);return()=>clearInterval(id)},[]);const timing=campaign.campaign_start_datetime&&campaign.campaign_end_datetime&&campaign.scheduled_minute!==null?campaignTiming(campaign,now):null,next=timing?.next,seconds=next?Math.max(0,Math.floor((next.getTime()-now.getTime())/1000)):0;return <section className="live-campaign"><p>#49 · REAL TIMES SQUARE · NYC</p><h1>{campaign.advertiser_name} IS {timing?.state==='live'?'🔴 LIVE':'LIVE IN TIMES SQUARE'}</h1>{campaign.creative_file_url?<img src={campaign.creative_file_url} alt={`${campaign.advertiser_name} campaign creative`}/>:<div className="physical-media-slot"><strong>{campaign.creative_headline||campaign.advertiser_name}</strong></div>}<h2>{timing?.state==='live'?'LIVE · TIMES SQUARE NYC':'NEXT TIMES SQUARE APPEARANCE'}</h2><strong>{next?next.toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit',timeZone:campaign.timezone}):'SCHEDULE COMPLETE'} ET</strong><div className="live-countdown">{String(Math.floor(seconds/60)).padStart(2,'0')}:{String(seconds%60).padStart(2,'0')}</div><p>Scheduled during the :{String(campaign.scheduled_minute).padStart(2,'0')} minute every hour in Times Square and on Timesquares.lol.</p><div><span>{timing?.completed||0} APPEARANCES COMPLETED</span> · <span>{timing?.remaining||24} REMAINING</span></div>{campaign.website&&<a className="primary-btn" href={campaign.website}>VISIT {campaign.advertiser_name} ↗</a>}<button className="primary-btn" onClick={()=>navigator.share?.({title:`${campaign.advertiser_name} is live in Times Square`,url:location.href})}>SHARE CAMPAIGN ↗</button></section>}
