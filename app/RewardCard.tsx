'use client';
import {useEffect,useState} from 'react';import {clerkToken} from './AuthControls';
const money=(c:number)=>`$${Math.round(c/100).toLocaleString()}`;
export default function RewardCard(){const [data,setData]=useState<any>(null);useEffect(()=>{let live=true;(async()=>{const token=await clerkToken();if(!token)return;const response=await fetch('/api/real-world/progress',{headers:{Authorization:`Bearer ${token}`}});if(response.ok&&live)setData(await response.json())})();return()=>{live=false}},[]);if(!data?.enabled||!data.progress)return null;const p=data.progress,unlocked=p.available>0;return <section className={`real-reward-card ${unlocked?'unlocked':''}`}><div className="reward-kicker">REAL TIMES SQUARE</div>{unlocked?<><h2>YOU&apos;RE GOING TO TIMES SQUARE</h2><p>You&apos;ve unlocked a Real Times Square Day.</p><a className="reward-cta" href="/dashboard/real-times-square">CLAIM YOUR BILLBOARD ↗</a></>:<><div className="reward-progress"><strong>{money(p.current)} <i>/ {money(p.type.qualifying_spend_required_cents)}</i></strong><span>{p.percent}% COMPLETE</span></div><div className="reward-bar"><i style={{width:`${p.percent}%`}}/></div><h2>{money(p.remaining)} MORE TO UNLOCK<br/>YOUR REAL TIMES SQUARE DAY</h2><p>Successful paid bids build permanent qualifying progress. Virtual rank and real-world eligibility are separate.</p></>}<div className="reward-counts"><span><b>{p.earned}</b>EARNED</span><span><b>{p.available}</b>AVAILABLE</span><span><b>{p.scheduled}</b>SCHEDULED</span><span><b>{p.completed}</b>COMPLETED</span></div></section>}
