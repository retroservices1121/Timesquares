'use client';import {useEffect,useState} from 'react';export default function RealWorldPromo(){const [data,setData]=useState<any>(null);useEffect(()=>{fetch('/api/real-world/public').then(r=>r.json()).then(setData)},[]);if(!data?.promoted)return null;return <aside className="real-world-promo"><button aria-label="Close" onClick={e=>(e.currentTarget.parentElement!.style.display='none')}>×</button><span>VIRTUAL → REAL</span><strong>THE VIRTUAL BILLBOARD<br/>CAN BECOME A REAL ONE.</strong><p>Reach ${(data.type.qualifying_spend_required_cents/100).toLocaleString('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0})} in qualifying spend and unlock a Real Times Square Day.</p><a href="/dashboard">SEE YOUR PROGRESS →</a></aside>}
