import {advertisers,advertiserSlug,minimumBid} from '../data';
export default function Leaderboard(){return <main className="inner-page"><header className="inner-nav"><a className="brand" href="/">TIMESQUARES<span>.LOL</span></a><a className="primary-btn" href="/dashboard/bid">GET A BILLBOARD ↗</a></header><section className="page-head"><p>LIVE RANKINGS · UPDATED NOW</p><h1>THE LEADERBOARD</h1><span>Open any advertiser profile or bid directly on any of the 30 screens.</span></section><section className="leader-table"><div className="table-row table-label"><span>RANK</span><span>ADVERTISER</span><span>CURRENT BID</span><span>BILLBOARD</span><span>HOLD TIME</span><span>ACTIONS</span></div>{advertisers.map((a,i)=>{const rank=i+1;return <div className="table-row" key={a[0]}><strong>#{rank}</strong><a className="advertiser-link" href={`/advertiser/${advertiserSlug(a[0])}`}>{a[0]}</a><span>${a[1].toLocaleString()}</span><span>{a[2]}</span><span>{a[3]}</span><span className="leader-actions"><a href={`/advertiser/${advertiserSlug(a[0])}`}>PROFILE</a><a href={`/dashboard/bid?slot=${rank}&amount=${minimumBid(a[1])}`}>BID ↗</a></span></div>})}</section></main>}
